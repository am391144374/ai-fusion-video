# 分镜场次 ID 解析修复完成总结

## 修复结果

- `storyboardSceneId=-1` 不再作为数据库主键查询。
- 当请求同时包含 `storyboardSceneId=-1` 和有效 `storyboardItemId` 时，系统从分镜条目解析真实场次并继续查询。
- 两个正整数 ID 同时存在但归属不一致时，返回明确错误，避免查询错误场次。
- 请求未提供任何正整数 ID 时，返回参数校验错误。

## 回归覆盖

- `sceneId=-1 + itemId=103` 成功解析条目所属场次。
- 有效场次 ID 与条目所属场次冲突时拒绝执行。
- 仅传入负数场次 ID 时返回正整数校验错误。

## 验证结果

- 补丁应用前检查：通过。
- Git 差异格式检查：通过。
- Maven 测试：当前环境未安装 Java/Maven，未执行。
