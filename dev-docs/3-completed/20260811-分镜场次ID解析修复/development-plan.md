# 分镜场次 ID 解析修复开发计划

## 问题

生成视频调用“查询场次镜头列表”时，模型可能同时传入占位场次 ID `-1` 与有效分镜条目 ID。现有逻辑把 `-1` 当成真实主键查询，导致“分镜场次不存在”。

## 目标

- 非正数场次 ID 视为未提供。
- 有效分镜条目 ID 可解析其真实所属场次。
- 同时提供两个有效 ID 时校验归属一致。
- 增加回归测试覆盖报错输入。

## 实施

1. 统一判断 ID 是否为正整数。
2. 优先读取有效分镜条目并取得真实场次 ID。
3. 拒绝互相冲突的有效场次 ID 与条目 ID。
4. 验证代码格式与测试场景。
