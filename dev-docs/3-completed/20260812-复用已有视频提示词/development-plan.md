# 复用已有视频提示词开发计划

## 问题

分镜生成视频流程无条件重新编写 `videoPrompt`，会覆盖用户已经生成或手工调整的提示词。

## 目标

- 普通生成视频时优先原样复用非空提示词。
- 已有提示词不得重新编写或覆盖。
- 仅生成提示词模式跳过已有提示词的镜头。
- 提示词为空时保持原有 H3 生成流程。

## 实施

1. 在执行 Agent 查询目标镜头后增加已有提示词分支。
2. 复用时仅保存视频地址，不回写提示词字段。
