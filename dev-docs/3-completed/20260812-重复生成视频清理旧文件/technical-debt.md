# 重复生成视频清理旧文件技术债

## 当前限制

旧视频只保存了访问 URL，没有记录创建它的存储配置 ID。为了避免误删，本次只删除能够确认属于当前默认存储配置的文件；外部 URL 或旧存储配置产生、当前无法确认归属的 URL 会保留并记录日志。

## 后续方向

如需跨存储配置自动清理历史文件，应在媒体引用中持久化存储配置 ID 与对象 key，再按明确归属删除，不能仅凭 URL 猜测。

当前执行环境没有 Java/Maven，新增测试尚未在本机实际运行，应由具备 JDK 的开发环境或 CI 执行。
