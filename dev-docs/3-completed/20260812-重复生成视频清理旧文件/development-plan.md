# 重复生成视频清理旧文件开发计划

## 问题

分镜重复生成视频后仅替换数据库 URL，旧视频原文件继续保留，占用本地磁盘或对象存储空间。

## 目标

- 新视频成功写回分镜后删除旧视频文件。
- 数据库更新失败时保留旧文件。
- 新旧 URL 相同、外部 URL 或无法确认归属的文件不得删除。
- 同时支持本地存储和 S3 兼容存储。

## 实施

1. 为统一存储策略增加安全删除能力。
2. 在分镜视频更新成功后触发旧文件删除。
3. 删除失败记录错误但不回滚已经成功的新视频。
