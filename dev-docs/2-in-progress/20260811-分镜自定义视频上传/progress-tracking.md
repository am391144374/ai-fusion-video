# 进度跟踪

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 现有链路梳理 | 已完成 | AI 生成结果通过既有更新接口保存至 `generatedVideoUrl`。 |
| 方案确认 | 已完成 | 用户确认采用列表内上传方案，且仅支持 MP4。 |
| 设计文档评审 | 已完成 | 已确认服务端 MP4 白名单和既有保存字段复用方案。 |
| 开发 | 已完成 | 已接入表格、卡片上传入口，上传后保存 `generatedVideoUrl`；合成同样优先该字段。 |
| 前端验证 | 已完成 | 完整 TypeScript 类型检查通过；定向 ESLint 无错误，仅有页面既有 4 条 Hook 依赖警告。 |
| 后端验证 | 阻塞 | 项目要求 Java 21；本机仅有 Java 17，且仓库现有代码使用 Java 21 预览语法，Maven 无法进入定向单元测试。 |
| 上传图标视觉调整 | 进行中 | 已确认与“生成视频”图标保持相同的样式和动画，等待设计文档复核。 |
